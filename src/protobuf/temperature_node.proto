syntax = "proto3";
import "device_discover.proto";

message TemperatureNodeReport {
    repeated TemperatureRecord temperature_record= 1;
    float battery_vcc = 2;
    uint64 device_mac = 3;
    uint32 measure_interval = 4;
}
